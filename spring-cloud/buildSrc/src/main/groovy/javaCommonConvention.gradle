plugins {
    id 'java'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

group = 'com.albert'
version = '0.0.2-SNAPSHOT'

tasks.named('test') {
    useJUnitPlatform()
}